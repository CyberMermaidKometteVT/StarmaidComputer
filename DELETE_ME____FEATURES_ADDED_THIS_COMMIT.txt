* Switching to OpenAI TTS instead of Microsoft Speech Synthesizer (which is garbo)
* Changing OpenAI library
* Updating a few packages
* Adding Send To Chat command
* Fixing SayLastFollwers (not outputting to my output window, didn't say all the followers)

TODO:
* "Text box is fucky"
* TTS for commands is sometimes missing naming parameters?
* Chat messages don't necessarily occur in the right order
* Code block formatting removes all text between the start of the FIRST code block, and the end of the LAST code block. Shouldn't do that!


